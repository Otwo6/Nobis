import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import ConstituentBridge from './PublicProblems';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ConstituentBridge />
  </React.StrictMode>
);